import SwiftUI
import CoreData

struct RootView: View {
    var body: some View {
        Text("FitTracker")
            .font(.largeTitle)
            .fontWeight(.semibold)
    }
}

#Preview {
    RootView()
}
